# 📚 Book Search API with FastAPI

This project is a **simple API for searching books** built with **FastAPI**.  
You can search books by **title, author, or publisher** using query parameters.  
A **simple HTML UI** is also included to search directly from your browser.

---

## ⚡ Features

- Search in **title, author, and publisher**  
- Standard **JSON output** for API  
- Automatic **Swagger documentation** at `/docs`  
- Simple UI with HTML and JavaScript  
- Ready to run in a **virtual environment (venv)**  

---

## 📁 Files

- `main.py` – FastAPI application  
- `books.json` – Book dataset  
- `index.html` – Simple search UI  
- `requirements.txt` – List of required Python packages  

---

## 🛠️ Installation & Running

1. **Create and activate a virtual environment**:

```bash
python -m venv venv
# Windows (cmd)
venv\Scripts\activate
# Mac / Linux
source venv/bin/activate
